deployment:
    image: quay.io/user1/simple-quarkus
    version: 1.2.0
    replicas: 1
    includeHealthChecks: true

route:
    host: simple-article-helm.apps-crc.testing

config:
    greeting: 'This needs to be changed per stage!'

